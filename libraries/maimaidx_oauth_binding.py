"""Deprecated: OAuth binding metadata is now managed by the sync plugin.

This module is kept as an empty stub so that existing tests and any
accidental imports do not break.  All symbols have been removed; the
sync plugin's ``user_binds.fish`` field is the single source of truth
for OAuth binding state.
"""
